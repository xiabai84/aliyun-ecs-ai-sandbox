---
- name: Install tensorflow with GPU and CPU
  shell: '{{conda_directory}}/bin/pip install tensorflow'
  become_user: ml

- name: Install tensorboard
  shell: '{{conda_directory}}/bin/pip install tensorboard'
  become_user: ml
